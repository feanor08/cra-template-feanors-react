module.exports = {
  printWidth: 120, // will ensure that your code does not exceed more then 100 characters
  singleQuote: true, // will convert all your double quotes into single quotes
  trailingComma: 'all', // will ensure there is a dangling comma at the end of last object property
  bracketSpacing: true, // prints spaces between object literals
  jsxBracketSameLine: false, // will put ">" of a multi line JSX element on the last line
  tabWidth: 2, // specifies the number of spaces per indentation level.
  semi: true, // will print ";"" at the end statements.
};
